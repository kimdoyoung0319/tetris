1. making current block more highlighted w/ different colors
2. refactor code
    wrapping some game.run() codes so making it more readable
3. make routine in game.run() that checks if the last line is full
4. implement is_game_end() function
5. re-write blocks by inheritance and making rotation more accurate
